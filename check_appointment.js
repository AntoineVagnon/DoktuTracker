// Quick script to check appointment data
const fetch = require('node-fetch');

async function checkAppointment() {
  try {
    // This would need authentication, so let me just check the code instead
    console.log('Checking appointment schema...');
  } catch (error) {
    console.error('Error:', error);
  }
}

checkAppointment();
